# Clock & Compass environment configuration.
# Copy this file to `.env` and fill in real values. NEVER commit `.env`.

# --- Database ---------------------------------------------------------------
# SQLite to start. Swap the provider + URL for Postgres later without code changes.
# SQLite example:   file:./dev.db   (resolved relative to packages/core/prisma)
# Postgres example: postgresql://user:password@localhost:5432/clockcompass?schema=public
DATABASE_URL="file:./dev.db"

# --- Server -----------------------------------------------------------------
# Port the Fastify server listens on (also serves the built frontend in prod).
PORT=3000
HOST=0.0.0.0
# Where the built frontend lives, relative to the server. Set in the Docker image.
# WEB_DIST_PATH=../web/dist

# --- Auth -------------------------------------------------------------------
# Bearer token for the REST API (single-user model). Generate a long random value.
API_AUTH_TOKEN="change-me-to-a-long-random-token"

# --- Web push (optional) -------------------------------------------------------
# Generate once with: pnpm --filter @clock-compass/server exec web-push generate-vapid-keys
# Leave blank to disable push notifications (the app works fine without them).
VAPID_PUBLIC_KEY=""
VAPID_PRIVATE_KEY=""
# VAPID_SUBJECT="mailto:you@example.com"

# --- AI ----------------------------------------------------------------------
# The provider sits behind an interface; pick one (or none — AI is optional).
# Force a provider with AI_PROVIDER=anthropic|openai-compatible|codex; otherwise
# it is inferred from which keys are set (Anthropic wins when both are set). The
# codex provider is never inferred — it must be selected explicitly.
# AI_PROVIDER=""

# Option A: Anthropic. Server-side only. Never commit.
ANTHROPIC_API_KEY=""
# Optional model override (defaults to claude-opus-4-8).
# ANTHROPIC_MODEL="claude-opus-4-8"

# Option B: any OpenAI-compatible endpoint — OpenAI itself, a local model via
# Ollama/LM Studio/vLLM, or a gateway (OpenRouter, LiteLLM). The API key is
# optional (local runtimes need none); the model name is required.
# OPENAI_BASE_URL="http://localhost:11434/v1"   # e.g. Ollama
# OPENAI_API_KEY=""
# OPENAI_MODEL="llama3.2"

# Option C: your ChatGPT Plus/Pro subscription via Codex "Sign in with ChatGPT".
# No API key here — set AI_PROVIDER=codex, then connect your account in the app
# (Settings → AI → Sign in with ChatGPT). Tokens are stored in the database.
# Optional model override (defaults to gpt-5):
# AI_PROVIDER="codex"
# CODEX_MODEL="gpt-5"
